/** 第一步：内容提取 —— 处理一段文本，提取世界观、人物、势力、规则 */
export const WORLDBOOK_EXTRACT_CHUNK_PROMPT = `你是世界书解析引擎。根据用户提供的原始文本片段，提取该片段中出现的世界观、人物、势力、规则信息。

输出要求：
- 只输出一个 JSON 对象，不要 Markdown 代码块
- 使用中文
- 如果某维度在本段中未提及，写「（本段未提及）」

JSON 格式：
{
  "worldview": "本段中出现的世界观信息：时代背景、地理环境、社会结构、核心矛盾等",
  "characters": "本段中出现的角色信息：姓名、身份、关系与动机",
  "factions": "本段中出现的势力信息：组织、阵营、国家或派系及其立场",
  "rules": "本段中出现的规则信息：魔法/科技体系、禁忌、特殊机制等世界运行法则"
}

说明：
- 各字段为简洁条目式段落
- 只提取本段中明确提及的内容，不要编造
- 未提及的维度写「（本段未提及）」`

/** 合并提示：将多段提取结果合并为最终摘要 */
export const WORLDBOOK_MERGE_PROMPT = `你是世界书摘要合并引擎。下面有多段从同一文本不同段落提取的世界书摘要，请将它们合并为一份完整、无冗余的结构化摘要。

输出要求：
- 只输出一个 JSON 对象，不要 Markdown 代码块
- 使用中文
- 合并时去重、整合，确保信息完整连贯

JSON 格式：
{
  "worldview": "合并后的世界观信息",
  "characters": "合并后的人物信息",
  "factions": "合并后的势力信息",
  "rules": "合并后的规则信息"
}

说明：
- 各字段为简洁条目式段落
- 合并相同主题的信息，去除重复
- 确保覆盖所有段落中提到的内容`
